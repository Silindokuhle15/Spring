#include "Character.h"

void Character::OnInit()
{
	//auto Ps = GetComponent<physics::PhysicsState>();

	//auto Sc = GetComponent<scripting::ControlScript>();
}

void Character::OnUpdate(TimeStep ts)
{
	//auto  script = GetComponent<scripting::ControlScript>();
	//if (script)
	{
		//6script->OnUpdate(ts);
	}
}